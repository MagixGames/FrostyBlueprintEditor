<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:nodes="clr-namespace:BlueprintEditorPlugin.Views.Nodes">

    <Style TargetType="{x:Type nodes:MinimapItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type nodes:MinimapItem}">
                    <Border Background="#666666"
                            BorderBrush="#999999"
                            BorderThickness="1"
                            Width="{Binding RelativeSource={RelativeSource AncestorType=nodes:MinimapItem}, Path=ActualWidth}"
                            Height="{Binding RelativeSource={RelativeSource AncestorType=nodes:MinimapItem}, Path=ActualHeight}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type nodes:Minimap}">
        <Setter Property="Background" Value="#1A1A1A" />
        <Setter Property="BorderBrush" Value="#505050" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type nodes:Minimap}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <!-- Nodes Canvas -->
                            <ItemsControl ItemsSource="{TemplateBinding ItemsSource}"
                                          x:Name="PART_ItemsHost">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#666666"
                                                BorderBrush="#999999"
                                                BorderThickness="1" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Viewport Rectangle -->
                            <Rectangle Fill="Transparent"
                                       Stroke="#FFFFFF"
                                       StrokeThickness="1.5"
                                       x:Name="ViewportRectangle" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="ViewportRectangle" Property="Stroke" Value="#888888" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>